name 'keyboard'
org 'akerl'

source(
  type: 'git',
  path: '.'
)

build do
  #run 'arduino-cli core update-index'
  #run 'arduino-cli core install keyboardio:avr'
  run 'arduino-cli compile --output-dir output'
  cp 'output/keyboard-latest.elf'
  cp 'output/keyboard-latest.hex'
end

package(
  type: 'file',
  artifacts: [
    {
      source: 'output/keyboard-latest.hex',
      name: 'keyboard.hex'
    },
    {
      source: 'output/keyboard-latest.elf',
      name: 'keyboard.elf'
    }
  ]
)

test do
  # TODO: add tests
end
